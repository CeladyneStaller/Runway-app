node_modules
dist
.DS_Store
*.local

# Real environment values. .env.example is the committed TEMPLATE and must stay blank —
# anything you paste there goes into version control.
.env
.env.*
!.env.example

# Secret bundles for `supabase secrets set --env-file` — real values, never committed.
*.secrets.env
stripe-secrets.env
