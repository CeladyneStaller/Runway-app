# Hosted sync is OPT-IN. With these unset (or VITE_SYNC_ENABLED not "true") the app runs entirely
# local-first against IndexedDB, which stays the fallback for the whole Phase 1 build.
VITE_SYNC_ENABLED=false
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
